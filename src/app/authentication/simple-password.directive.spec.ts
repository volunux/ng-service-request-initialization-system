import { SimplePasswordDirective } from './simple-password.directive';

describe('SimplePasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new SimplePasswordDirective();
    expect(directive).toBeTruthy();
  });
});
