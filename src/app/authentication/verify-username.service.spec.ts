import { VerifyUsernameDirective } from './verify-username.directive';

describe('VerifyUsernameDirective', () => {
  it('should create an instance', () => {
    const directive = new VerifyUsernameDirective();
    expect(directive).toBeTruthy();
  });
});
