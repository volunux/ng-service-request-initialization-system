import { ReplaceLineBreakPipe } from './replace-line-break.pipe';

describe('ReplaceLineBreakPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceLineBreakPipe();
    expect(pipe).toBeTruthy();
  });
});
