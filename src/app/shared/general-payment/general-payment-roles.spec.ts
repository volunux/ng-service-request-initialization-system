import { GeneralPaymentRoles } from './general-payment-roles';

describe('GeneralPaymentRoles', () => {
  it('should create an instance', () => {
    expect(new GeneralPaymentRoles()).toBeTruthy();
  });
});
