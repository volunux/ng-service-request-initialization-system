import { ReplaceTabsPipe } from './replace-tabs.pipe';

describe('ReplaceTabsPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceTabsPipe();
    expect(pipe).toBeTruthy();
  });
});
