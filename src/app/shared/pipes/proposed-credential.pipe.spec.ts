import { ProposedCredentialPipe } from './proposed-credential.pipe';

describe('ProposedCredentialPipe', () => {
  it('create an instance', () => {
    const pipe = new ProposedCredentialPipe();
    expect(pipe).toBeTruthy();
  });
});
