import { UserFormValidatorsDirective } from './user-form-validators.directive';

describe('UserFormValidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new UserFormValidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
